[project]
name = "category-promotion-analysis-backend"
version = "0.1.0"
description = "FastAPI backend for Category Promotion Analysis"
requires-python = ">=3.10"
dependencies = [
  "fastapi>=0.110",
  "uvicorn[standard]>=0.27",
  "pydantic>=2.6",
  "pandas>=2.0",
]

[tool.ruff]
line-length = 100